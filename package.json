{
  "name": "dingyue-docs",
  "version": "1.0.0",
  "description": "Dingyue 文档网站",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build && npm run copy-public && npm run add-favicon",
    "copy-public": "cp -r public/* .vitepress/dist/ 2>/dev/null || true",
    "add-favicon": "node .vitepress/add-favicon.js",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.0"
  }
}
